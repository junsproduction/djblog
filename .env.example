# Django Core Settings
SECRET_KEY=your-secret-key-here-generate-a-new-one
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Settings (Local Development)
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_HOST=localhost
DB_PORT=5432

# Production Database (Use one of these)
# POSTGRES_URL=postgres://username:password@host:5432/database  # Vercel Postgres
# DATABASE_URL=postgres://username:password@host:5432/database  # External service

# Email Configuration
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Security Settings (Production)
SECURE_SSL_REDIRECT=True

# CORS Settings (Production)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Vercel Settings (Auto-populated by Vercel)
# VERCEL_URL=your-app.vercel.app
# VERCEL_BRANCH_URL=your-branch-app.vercel.app